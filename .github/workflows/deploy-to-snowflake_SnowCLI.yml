name: Deploy to Snowflake (SnowCLI)

on:
  push:
    branches:
      - dev  # Trigger on push to dev branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python & Install Snowflake CLI
        run: |
          set -x
          python3 -m pip install --upgrade pip
          pip install snowflake-cli-labs
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          hash -r  # refresh shell path
          snow --version || echo "Snow CLI not found"

      - name: Debug Snowflake environment variables
        run: |
          set -x
          echo "🛠 Snowflake Environment Variables:"
          echo "SNOWFLAKE_ACCOUNT=$SNOWFLAKE_ACCOUNT"
          echo "SNOWFLAKE_USERNAME=$SNOWFLAKE_USERNAME"
          echo "SNOWFLAKE_PASSWORD=********"  # Mask password for security
          echo "SNOWFLAKE_ROLE=$SNOWFLAKE_ROLE"
          echo "SNOWFLAKE_WAREHOUSE=$SNOWFLAKE_WAREHOUSE"
          echo "SNOWFLAKE_DATABASE=$SNOWFLAKE_DATABASE"
          echo "SNOWFLAKE_SCHEMA=$SNOWFLAKE_SCHEMA"

      - name: Show deployment script info
        run: |
          set -x
          echo "📄 Displaying deployment SQL script info and first few lines:"
          ls -l scripts/main_deploy.sql
          head -n 20 scripts/main_deploy.sql

      - name: Run Snowflake Deployment (Inline Credentials)
        env:
          SNOWFLAKE_ACCOUNT: SPHEUCZ-HQ66596
          SNOWFLAKE_USERNAME: SFTRAINING
          SNOWFLAKE_PASSWORD: GoodSuperluck@025
          SNOWFLAKE_ROLE: ACCOUNTADMIN
          SNOWFLAKE_WAREHOUSE: COMPUTE_WH
          SNOWFLAKE_DATABASE: SFTRAINING_EMP_DB
          SNOWFLAKE_SCHEMA: EMP_HR
        run: |
          set -x
          echo "🔁 Running Snowflake deploy script via Snow CLI..."
      
          snow sql \
            --account "$SNOWFLAKE_ACCOUNT" \
            --user "$SNOWFLAKE_USERNAME" \
            --password "$SNOWFLAKE_PASSWORD" \
            --role "$SNOWFLAKE_ROLE" \
            --warehouse "$SNOWFLAKE_WAREHOUSE" \
            --database "$SNOWFLAKE_DATABASE" \
            --schema "$SNOWFLAKE_SCHEMA" \
            --filename scripts/main_deploy.sql
          ret=$?
          echo "Snowflake CLI exit code: $ret"
          if [ $ret -ne 0 ]; then
            echo "❌ Snowflake CLI execution failed"
            exit $ret
          else
            echo "✅ Snowflake CLI executed successfully"
          fi
